<view class="campus-map-page {{appearanceClass}}">
  <scroll-view class="campus-map-scroll" scroll-y enhanced show-scrollbar="{{false}}">
    <view class="map-head">
      <view class="back" bindtap="goBack">‹</view>
      <view><text class="map-title">玉泉开饭地图</text><text class="map-desc">校内导览图 · 点击地点查看食堂信息</text></view>
    </view>

    <view class="filter-row"><view wx:for="{{filters}}" wx:key="*this" class="{{activeFilter === item ? 'active' : ''}}" data-value="{{item}}" bindtap="selectFilter">{{index === 0 ? '⌖' : index === 1 ? '◷' : '♧'}} {{item}}</view></view>

    <movable-area class="map-stage">
      <movable-view class="map-canvas" direction="all" inertia out-of-bounds scale scale-min="1" scale-max="2" scale-value="1">
        <image class="campus-map" src="/static/map/zju-yuquan-3d-cartoon-demo.jpg" mode="aspectFill" />
        <view class="route-guide {{routeVisible ? 'route-guide--visible' : ''}}"></view>
        <view
          wx:for="{{eateries}}"
          wx:key="id"
          class="map-pin {{selected.id === item.id ? 'map-pin--active' : ''}}"
          style="left:{{item.left}}%;top:{{item.top}}%;"
          data-id="{{item.id}}"
          catchtap="selectEatery"
        >
          <view class="pin-dot">⌾</view><text>{{item.name}}</text>
        </view>
      </movable-view>
    </movable-area>

    <view class="eatery-card">
      <view class="grabber"></view>
      <view class="eatery-heading"><text>{{selected.name}}</text><text>◷ {{selected.open === false ? '已打烊' : '营业中'}}</text></view>
      <text class="eatery-subtitle">{{selected.subtitle}}</text>
      <view class="eatery-stats">
        <view><text class="people">{{selected.people}}</text><text>人</text><text>此刻人数</text></view>
        <view><text class="wait">{{selected.wait}}</text><text>{{selected.waitDetail}}</text></view>
      </view>
      <view class="walk-button" bindtap="startWalk">⌖ {{routeVisible ? '校内路线已标出' : '开始步行导航'}}　›</view>
    </view>
    <view class="map-scroll-space"></view>
  </scroll-view>
</view>
